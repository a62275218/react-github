import React from 'react';
import NewsTemplate from './common/NewsTemplate'

const TopNews = () => {
    return (
        <div>
            <NewsTemplate/>
        </div>
    );
};

export default TopNews;
